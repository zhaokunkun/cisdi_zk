# 如何使用 OpenSpec 进行 AI 编程

> 本文档基于一次真实的开发实践整理，帮助新人理解并掌握 OpenSpec 工作流。
> 重点在于**思维方式**，而非具体需求。

---

## 什么是 OpenSpec

OpenSpec 是一套**需求驱动的 AI 编程工作流**。它的核心思想是：

> 先想清楚做什么、怎么做，再动手写代码。

在没有 OpenSpec 的情况下，AI 容易"直接开写"，导致：
- 生成不需要的代码
- 代码风格与项目不一致
- 漏掉关键校验逻辑
- 无法追溯"为什么这样写"

OpenSpec 通过**8个步骤**，强制在编码前完成需求分析、技术设计和任务拆分，让 AI 的输出可控、可追溯、可复用。

---

## 8步工作流

### 第1步：理解需求

**做什么**：向 AI 描述你的需求，让它先阅读相关代码和文档，充分理解上下文。

**关键原则**：
- 不要一上来就让 AI 写代码
- 让 AI 先读现有代码，搞清楚项目结构、技术栈、已有的类似功能
- 主动告诉 AI 不确定的点

**实践提示**：
```
❌ "帮我写一个取消提交校验的接口"
✅ "我需要在分包验证模板中增加取消提交的校验逻辑，
    请先看一下现有的 InValidationTempLineController 和
    OutValTempLineController 的代码结构"
```

---

### 第2步：创建 OpenSpec 目录

**做什么**：在项目的 `specs/` 目录下创建本次变更的专属目录。

**命名规则**：`YYYY-MM-DD-功能描述`

**目录结构**：
```
specs/
└── 2026-04-23-cancel-commit-validation/
    ├── README.md        # 变更总览
    ├── proposal.md      # 变更提案
    ├── design.md        # 技术设计
    ├── tasks.md         # 任务清单
    └── archive.md       # 归档
```

**为什么要这样做**：
- 每次变更有独立空间，方便追溯
- 文档和代码放在一起，不会丢失上下文
- 后续 review 时有据可查

---

### 第3步：编写 proposal.md（变更提案）

**做什么**：用自然语言描述"为什么要做、做什么、不做什么"。

**必填章节**：

| 章节 | 说明 | 示例 |
|------|------|------|
| 背景 | 为什么需要这个变更 | "当前取消提交没有任何校验，可能导致已引用的模板被误取消" |
| 目标 | 要达成什么效果 | "取消提交前检查是否有验证单引用了该模板" |
| 范围 | 包含/不包含 | "包含：进场/退场模板的校验；不包含：模板删除功能" |
| 需求描述 | 功能列表 + 校验规则 | "接口返回 Boolean，true=可取消，false=不可取消" |
| 风险评估 | 可能的风险 | "查询验证单表数据量大时可能慢，需加索引" |
| 里程碑 | 各阶段计划 | "4/23 完成开发，4/24 部署测试" |

**关键原则**：
- 范围要**明确边界**，写清楚"不包含什么"比"包含什么"更重要
- 校验规则要**写具体**，不要模糊描述

---

### 第4步：编写 design.md（技术设计）

**做什么**：将 proposal 翻译成技术方案。

**必填章节**：

| 章节 | 说明 |
|------|------|
| 概述 | 需求说明 + 设计范围 |
| 技术方案 | 用什么技术、怎么分层 |
| 接口设计 | URL、请求参数、响应格式 |
| 核心逻辑 | 各层伪代码（Controller → Service → Repository） |
| 数据库设计 | 涉及的表、字段、索引 |
| 上下游依赖 | 依赖的外部系统 |

**接口设计示例**：
```
GET /minmetals-pm-pem-sbc/api/in-val-temp-line/canCancelCommit

请求参数：code (String) - 模板编码
响应格式：{ "code": "0", "data": true }
```

**核心逻辑示例**：
```
Controller:
  接收 code 参数，调用 Service.canCancelCommit(code)，返回结果

Service:
  调用 Repository.existByCode(code)
  返回 !existByCode 结果（不存在引用 = 可取消）

Repository:
  用 LambdaQueryWrapper 查询验证单表
  条件：sub_in_validation_temp_code = code
  返回 exists 结果
```

**关键原则**：
- 伪代码写到**方法级别**即可，不需要写具体实现
- 接口设计要包含**完整的 URL、参数、返回值**
- 数据库设计要注明**哪些表、哪些字段参与查询**

---

### 第5步：编写 tasks.md（任务清单）

**做什么**：将设计拆成可执行的小任务。

**拆分原则**：按分层顺序，从底向上

```
1. Entity      → 定义数据结构
2. Mapper      → 定义数据访问
3. Repository  → 封装数据操作
4. Service     → 实现业务逻辑
5. Controller  → 暴露接口
6. 数据库索引   → 记录即可，不立即执行
```

**每个任务包含**：
- 负责人（AI 或人）
- 预估时间（不超过 0.5h）
- 详细描述（要写什么文件、什么方法）

**任务示例**：
```markdown
### [ ] 任务1: 创建 Entity 类
- 负责人：AI
- 预估时间：10min
- 描述：创建 InValidationLineEntity 和 OutValLineEntity，
       只需包含 canCancelCommit 查询用到的字段

### [ ] 任务2: 创建 Mapper 接口
- 负责人：AI
- 预估时间：5min
- 描述：创建 InValidationLineMapper 和 OutValLineMapper，
       继承 BaseMapper，暂无需自定义 SQL
```

**末尾附加测试用例表格**：
```markdown
| 编号 | 场景 | 输入 | 期望输出 |
|------|------|------|----------|
| TC001 | 进场模板-未被引用 | code=TEMPLATE_A | true |
| TC002 | 进场模板-已被引用 | code=TEMPLATE_B | false |
| TC003 | 参数为空 | code="" | true |
```

**关键原则**：
- 任务粒度要**细**，每个任务不超过 30 分钟
- 只拆**本次需求相关**的任务，不要加额外内容
- 数据库索引记录到任务清单即可，由 DBA 或上线时统一处理

---

### 第6步：用户 Review

**做什么**：将 proposal + design + tasks 交给用户确认。

**确认要点**：
1. 需求范围是否正确（有没有多做的、有没有漏做的）
2. 接口设计是否合理（URL、参数、返回值）
3. 任务拆分是否合理（粒度是否合适、顺序是否正确）

**⚠️ 这一步不能跳过**。未经确认直接开发，后续返工成本更高。

---

### 第7步：按 tasks.md 顺序开发代码

**做什么**：严格按任务清单顺序，逐个完成编码。

**执行原则**：
- **按顺序**：Entity → Mapper → Repository → Service → Controller
- **每完成一个任务**：更新 tasks.md 中的状态（`[ ]` → `[x]`）
- **只生成本次相关代码**：不要顺手加额外的方法或逻辑
- **对比原始代码**：生成后与现有项目代码对比，确保风格一致

**实践中的教训**：
1. **import 路径要一致** — 生成代码用了错误的 import 路径（`lingshu` vs `elite.forge`），导致编译失败
2. **注解不能遗漏** — 原始代码每个字段都有 `@TableField(value = "xxx", keepGlobalFormat = true)`，生成时遗漏了
3. **serialVersionUID 不能省** — 规范要求 POJO 实现 Serializable，必须有 serialVersionUID
4. **不要生成多余代码** — 顺手生成了 getCount、getDataById 等原有方法，反而增加了 review 负担

**建议的对话方式**：
```
"按 tasks.md 顺序开始开发，从任务1开始"
"任务1完成了，继续任务2"
"对比一下生成的代码和原始代码有没有差异"
```

---

### 第8步：完成后填写 archive.md

**做什么**：归档本次变更的实际执行情况。

**记录内容**：
- 实际完成时间
- 遇到的问题
- 经验总结

**示例**：
```markdown
## 归档

- 完成时间：2026-04-23
- 实际耗时：1h（含 review 和修复）
- 遇到的问题：
  1. Entity import 路径错误，需与项目现有代码保持一致
  2. @TableField 注解遗漏，需按原始代码补全
  3. 生成了多余的方法，需清理只保留本次需求相关代码
- 经验总结：
  1. 生成代码后务必与原始代码对比差异
  2. 只生成本次需求相关的代码，不加额外内容
  3. 数据库索引不随代码开发，记录到任务清单即可
```

---

## 速查清单

| 步骤 | 关键动作 | 产出物 |
|------|----------|--------|
| 1. 理解需求 | 读代码、确认疑问 | — |
| 2. 创建目录 | `specs/YYYY-MM-DD-功能描述/` | 目录结构 |
| 3. proposal | 背景、目标、范围、规则 | proposal.md |
| 4. design | 接口、逻辑、数据库 | design.md |
| 5. tasks | 按分层拆任务 + 测试用例 | tasks.md |
| 6. Review | 用户确认后才能开发 | — |
| 7. 开发 | 按顺序编码、更新状态 | 代码文件 |
| 8. 归档 | 记录问题、经验 | archive.md |

---

## 常见问题

### Q: 每次都要走完8步吗？
小改动（改个字段、修个 bug）可以简化。但**涉及新接口、新逻辑**的变更，建议走完整流程。

### Q: AI 生成的代码可以直接用吗？
**不可以**。必须：
1. 与项目现有代码对比差异
2. 检查 import 路径、注解、规范是否一致
3. 清理不属于本次需求的代码

### Q: 测试用例怎么执行？
AI 负责生成测试用例和 curl 命令，开发者自行部署后验证。

### Q: 数据库索引什么时候加？
记录到 tasks.md 中，由 DBA 或上线时统一处理，不随代码一起开发。

### Q: OpenSpec Skill 放在哪里？
项目根目录下的 `.codebuddy/rules/openspec-workflow.mdc`，AI 会自动加载。
