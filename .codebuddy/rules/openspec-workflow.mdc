---
description: OpenSpec 工作流规范 - 用于规范化地完成从需求分析到代码实现的全过程
alwaysApply: true
enabled: true
---

# OpenSpec 工作流规范

## 何时使用
当需要开发新功能或变更需求时，使用 OpenSpec 工作流来规范化地完成从需求分析到代码实现的全过程。

## 工作流步骤

### 第1步：理解需求
- 阅读需求文档、设计文档、现有代码
- 向用户确认不确定的点
- 充分理解后再动手

### 第2步：创建 OpenSpec 目录
在项目的 `specs/` 目录下创建变更目录，命名格式：`YYYY-MM-DD-功能描述`

```
specs/
└── YYYY-MM-DD-功能描述/
    ├── README.md        # 变更总览（目录、状态、链接）
    ├── proposal.md      # 变更提案（背景、目标、范围、规则）
    ├── design.md        # 技术设计（接口、逻辑、数据库）
    ├── tasks.md         # 任务清单（按分层拆分，含测试用例）
    └── archive.md       # 归档（完成后填写）
```

### 第3步：编写 proposal.md（变更提案）
必填章节：
1. **背景** — 为什么需要这个变更
2. **目标** — 要达成什么效果
3. **范围** — 包含/不包含的内容
4. **需求描述** — 功能需求列表 + 校验规则
5. **风险评估** — 可能的风险和应对
6. **里程碑** — 各阶段计划时间

### 第4步：编写 design.md（技术设计）
必填章节：
1. **概述** — 需求说明 + 设计范围
2. **技术方案** — 技术栈 + 模块设计
3. **接口设计** — 接口列表 + 请求/响应详情
4. **核心逻辑** — Controller / Service / Repository 各层伪代码
5. **数据库设计** — 关联关系 + 索引要求
6. **上下游依赖** — 依赖的外部系统或服务

### 第5步：编写 tasks.md（任务清单）
按分层顺序拆分任务：
1. Entity → 2. Mapper → 3. Repository → 4. Service → 5. Controller → 6. 数据库索引（索引任务记录到任务清单即可，不需要立即执行）
每个任务包含：负责人、预估时间、详细描述
末尾附加测试用例表格

### 第6步：用户 Review
将 proposal / design / tasks 提交给用户 review，确认无误后再开发

### 第7步：按 tasks.md 顺序开发代码
严格按照任务清单顺序实现，每完成一个任务更新 tasks.md 中的状态

### 第8步：完成后填写 archive.md
归档实际完成时间、遇到的问题、经验总结

## 注意事项
- 所有代码必须遵循 AGENTS.md 中的规范
- proposal 和 design 要经过用户确认后才能开发
- 任务拆分要细粒度，每个任务不超过 0.5h
- 测试用例必须覆盖正常和异常场景
- 数据库索引任务只需记录到任务清单，不随代码一起开发，由 DBA 或上线时统一处理
- 接口测试由开发者自行执行验证，AI 负责生成测试用例和 curl 命令
